data_source:
  type: "postgres"
  table: "flights"

quality:
  max_schema_violation_rate: 0.01
  max_missing_rate: 0.05

model:
  type: "xgboost"
  model_path: "models/ml_advanced/xgboost_tuned.joblib"

drift:
  threshold_ks_stat: 0.1

dashboard:
  host: "0.0.0.0"
  port: 8000

artifacts:
  output_dir: "reports/projects/ops_anomaly_system"

